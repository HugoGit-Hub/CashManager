import 'package:flutter/material.dart';

class ScanHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Scan Home page'),
      ),
      body: Center(
        child: Text('This is Scan Home page'),
      ),
    );
  }
}